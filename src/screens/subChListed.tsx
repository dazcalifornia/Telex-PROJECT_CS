import React ,{
  useState,
  useEffect,
  useRef,
  useCallback,
} from 'react';

import {
  View,
  Text,
  ScrollView,
  Box,
} from 'native-base';

import {auth, db} from '../firebase';

const SubChListed = () => {
  return (
    <View>
      <Text>SubChListed</Text>
    </View>
  )
}
export default SubChListed;
